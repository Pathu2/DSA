#include<bits/stdc++.h>
using namespace std;
int x;
vector<int> moves;
int n;

int rec(int x)
{
    //basecase
    if ()
    {
        /* code */
    }
    
    //cache check

    //compute

    //return
}

void solve(){
    cin>>n;
    moves.resize(n);
    for (int i = 0; i < n; i++)
    {
        moves[i];
    }

    
}
signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);

    int t=1; while(t--){
        solve();
    }
    return 0;
}